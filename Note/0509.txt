<리눅스>
 - Linux, Cloud
 Linux : SentOS(Version:7,8 --> 보안성 ↑), Debian, Ubuntu
 Cloud : AWS(Free Tier)

 Linux -> Docker

 VirtualBox
 hackintosh

서버 포트를 4개 오픈
ssh : 22
web : 80, 8080, 8000
DNS : 53
MySQL, MariaDB : 3306


포트 개방
ufw 포트를 open/close 할 때 사용하는 명령어

~# ufw allow 22
~# ufw allow 80
~# ufw allow 8080
~# ufw allow 8000
~# ufw allow 3306

가상머신에서 포트
NAT(포트와 포트를 연결)

ssh_rule
web80_rule
web8080_rule
web8000_rule
mysql_rule

서버가 정상적으로 열려있는지 확인
<cmd>
> ssh
> ipconfig

ssh 서비스 접속 프로그램 설치
~# apt-get install openssh-server
> ssh ai@192.168.56.1  # ssh id@ipadd

Django 프레임워크로 가동

~# apt install python3-pip  

Django 설치 웹포트 테스트
pip install django==4.0

top : 서버 상태 확인

data rollback --> /var/lib/mysql

SELECT문 예시
SELECT show_id, type, title, director, release_year FROM netflix where country = 'South Korea' and release_year<2022 limit 20;
SELECT show_id, type, title, director, release_year FROM netflix where country = 'South Korea' and release_year<=2022 order by release_year desc;
SELECT show_id, type, title, director, release_year FROM netflix where title like '%Naruto%' order by release_year desc;
SELECT show_id, type, title, director, release_year FROM netflix where release_year in(2015, 2016, 2020) order by release_year asc;

SELECT count(*) FROM netflix
SELECT count(*) as total FROM netflix

